<div class="container px-6 mx-auto mt-10 grid">
  <%= render 'layouts/flash' %>

  <%= render @test_run %>

  <div class="w-full w-full mx-auto mt-10 overflow-hidden rounded-lg shadow-xs">
    <h1 class="font-bold text-4xl">Models Runs</h1>
    <div class="w-full overflow-x-auto">
      <table class="w-full whitespace-no-wrap">
        <thead>
          <tr
            class="text-xs font-semibold tracking-wide text-left text-gray-500 uppercase border-b dark:border-gray-700 bg-gray-50 dark:text-gray-400 dark:bg-gray-800"
          >
            <th class="px-4 py-3">ID</th>
            <th class="px-4 py-3">Model</th>
            <th class="px-4 py-3">Model Version</th>
            <th class="px-4 py-3">State</th>
            <th class="px-4 py-3">Created at</th>
            <% if @test_model_version_runs.any? { |test_model_version_run| !test_model_version_run.performed? } %>
              <th class="w-1"></th>
            <% end %>
          </tr>
        </thead>
        <tbody
          class="bg-white divide-y dark:divide-gray-700 dark:bg-gray-800"
        >
          <% if @test_model_version_runs.empty? %>
            <tr>
              <td colspan="5" class="px-4 py-3 text-center">
                <p class="text-gray-500">No model versions runs found.</p>
              </td>
            </tr>
          <% else %>
            <% @test_model_version_runs.each do |test_model_version_run| %>
              <tr class="text-gray-700 dark:text-gray-400">
                <td class="px-4 py-3 text-sm">
                  <p class="font-semibold"><%= link_to test_model_version_run.id, [@test_run, test_model_version_run], class: "text-blue-500 underline-link" %></p>
                </td>
                <td class="px-4 py-3 text-sm">
                  <p class="font-semibold"><%= link_to test_model_version_run.model_version.model.name, test_model_version_run.model_version.model, class: "text-blue-500 underline-link" %></p>
                </td>
                <td class="px-4 py-3 text-sm">
                  <p class="font-semibold"><%= link_to test_model_version_run.model_version.build_name, [test_model_version_run.model_version.model, test_model_version_run.model_version], class: "text-blue-500 underline-link" %></p>
                </td>
                <td class="px-4 py-3 text-xs">
                  <% state = test_model_version_run.state %>
                  <span
                    class="px-2 py-1 font-semibold leading-tight rounded-full <%= define_status_class(state) %>"
                  >
                     <%= state.capitalize %> <%= test_model_version_run.passed_percentage.present? ? "(#{test_model_version_run.passed_percentage})%" : '' %>
                  </span>
                </td>
                <td class="px-4 py-3 text-sm">
                  <%= time_ago_in_words(test_model_version_run.created_at) %> ago
                </td>
                <% unless test_model_version_run.performed? %>
                  <td class="px-4 py-2">
                    <%= button_to 'Run', perform_test_run_test_model_version_run_path(@test_run, test_model_version_run), method: :post, class: 'bg-blue-500 text-white font-bold py-2 px-4 rounded' %>
                  </td>
                <% end %>
              </tr>
            <% end %>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
